import { CcsClickOutsideDirective } from './ccs-click-outside.directive';

describe('CcsClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new CcsClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
